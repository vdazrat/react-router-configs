## Centralized configuration for react-router

This is similar to react-router/react-router-configs, but instead all the routes are generated from the config,  
i.e the route prop is not passed into components.
